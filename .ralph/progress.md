- 2026-03-18 — Item 1 (install/package): bumped `glimpseui` to `^0.5.1`, replaced helper-building `scripts/postinstall.js` with upstream Glimpse dependency verification, routed Linux backend selection to `glimpse`, and excluded `.pi/extensions/generative-ui/native/linux/**` from npm package contents. Files changed: `package.json`, `package-lock.json`, `scripts/postinstall.js`, `.pi/extensions/generative-ui/backend/index.ts`, `.pi/extensions/generative-ui/backend/glimpse.ts`, `.ralph/items.json`, `.ralph/progress.md`. Verification: `npm install` ✅ (no `EBADPLATFORM`, reports `glimpseui@0.5.1`); `npm pack --dry-run` ✅ (helper artifacts omitted); `tsc --noEmit --module es2022 --moduleResolution bundler --target es2022 --skipLibCheck .pi/extensions/generative-ui/index.ts .pi/extensions/generative-ui/backend/*.ts` ✅; `tsc --allowJs --checkJs --noEmit scripts/*.js` ✅; `node --check scripts/*.js` ✅; backend probe `kind` => `glimpse` ✅; backend `checkSupport()` => `BACKEND_BINARY_MISSING` with actionable upstream build guidance because this environment lacks the built Glimpse host. Next: work item 2 to harden Glimpse support/error normalization and validate on a Linux environment where upstream Glimpse has been built successfully.
- 2026-03-18 — Item 2 (backend/support): hardened Linux Glimpse diagnostics by adding startup probing and stable normalization for missing module/host, non-executable host, display/WSLg, GTK/WebKit runtime, and generic startup failures, plus a `PI_GENERATIVE_UI_TEST_WSL` override for deterministic probes. Files changed: `.pi/extensions/generative-ui/backend/glimpse.ts`, `scripts/verify-glimpse-fallback.js`, `.ralph/items.json`, `.ralph/progress.md`. Verification: `npm install` ✅; `npm pack --dry-run` ✅; `tsc --noEmit --module es2022 --moduleResolution bundler --target es2022 --skipLibCheck .pi/extensions/generative-ui/index.ts .pi/extensions/generative-ui/backend/*.ts` ✅; `tsc --allowJs --checkJs --noEmit scripts/*.js` ✅; `node --check scripts/*.js` ✅; `npx --yes tsx -e "import { getWidgetBackend } from './.pi/extensions/generative-ui/backend/index.ts'; const backend = getWidgetBackend(); console.log(JSON.stringify({ kind: backend.kind }));"` ✅ (`kind=glimpse`); `npx --yes tsx -e "import { getWidgetBackend } from './.pi/extensions/generative-ui/backend/index.ts'; const backend = getWidgetBackend(); backend.checkSupport().then((result) => console.log(JSON.stringify(result, null, 2)));"` ⚠️ (`BACKEND_BINARY_MISSING` because upstream Glimpse was not built here); `node scripts/verify-glimpse-fallback.js` ✅. Next: run item 2/item 3 acceptance on a Linux environment with GTK4/WebKitGTK dev packages installed so upstream Glimpse can build and a real `{ ok: true }` headed Glimpse path can be verified.
- 2026-03-18 — Item 2 (backend/support): tightened Linux Glimpse remediation guidance with explicit distro package names and added `scripts/verify-linux-glimpse-support.js` to probe real repo-state readiness (`display`, cargo, pkg-config deps, host path, backend kind, `checkSupport()`). Files changed: `.pi/extensions/generative-ui/backend/glimpse.ts`, `scripts/verify-linux-glimpse-support.js`, `.ralph/items.json`, `.ralph/progress.md`. Verification: `npm install` ✅; `npm pack --dry-run` ✅; `tsc --noEmit --module es2022 --moduleResolution bundler --target es2022 --skipLibCheck .pi/extensions/generative-ui/index.ts .pi/extensions/generative-ui/backend/*.ts` ✅; `tsc --allowJs --checkJs --noEmit scripts/*.js` ✅; `node --check scripts/*.js` ✅; backend probe `kind` => `glimpse` ✅; backend `checkSupport()` ⚠️ `BACKEND_BINARY_MISSING`; `node scripts/verify-glimpse-fallback.js` ✅; `node scripts/verify-linux-glimpse-support.js` ⚠️ expected fail, reporting missing `libgtk-4-dev`, `libwebkitgtk-6.0-dev`, and `libgtk4-layer-shell-dev`; `npm --prefix node_modules/glimpseui run build:linux` ❌ with the same missing-package blocker. Next: rerun item 2 on a Linux environment where those packages can be installed and the upstream Glimpse host can actually be built, then verify a real `{ ok: true }` support check before attempting headed item 3 acceptance.
- 2026-03-18 — Item 2 (backend/support): revalidated the current WSLg repo state instead of changing runtime code because item 2 is still blocked on upstream Glimpse host build prerequisites. Files changed: `.ralph/items.json`, `.ralph/progress.md`. Verification: `pwd` ✅ (`/home/devkit/.pi/agent/extensions/pi-generative-ui`); `git log --oneline -10` ✅; `git status --short` ✅ (pre-existing dirty state includes `.ralph/plan.md`, `.pi/plans/`, `.pi/tasks/`, `.ralph/loop.md`, `.ralph/prompt.md`); `npm install` ✅ (postinstall still skips Linux host build); `npm pack --dry-run` ✅; `tsc --noEmit --module es2022 --moduleResolution bundler --target es2022 --skipLibCheck .pi/extensions/generative-ui/index.ts .pi/extensions/generative-ui/backend/*.ts` ✅; `tsc --allowJs --checkJs --noEmit scripts/*.js` ✅; `node --check scripts/*.js` ✅; backend probe `kind` => `glimpse` ✅; backend `checkSupport()` ⚠️ `BACKEND_BINARY_MISSING`; `node scripts/verify-glimpse-fallback.js` ✅; `node scripts/verify-linux-glimpse-support.js` ❌ (display/WSLg/cargo/pkg-config present but Linux pkg-config deps still missing); `npm --prefix node_modules/glimpseui run build:linux` ❌ (upstream build still blocked by missing `libgtk-4-dev`, `libwebkitgtk-6.0-dev`, `libgtk4-layer-shell-dev`); `sudo -n true` ❌ (password required, so this environment cannot install the missing packages autonomously). Next: rerun item 2 in a Linux environment with those system packages preinstalled or with sudo access, confirm `backend.checkSupport()` returns `{ "ok": true }`, then move to headed item 3 acceptance.
