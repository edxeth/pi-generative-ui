{
  "version": 1,
  "items": [
    {
      "category": "functional",
      "description": "Linux package installation succeeds on WSL2 Ubuntu 24 with WSLg and required GTK/WebKitGTK development packages, without `EBADPLATFORM`, and produces an executable Linux helper binary.",
      "steps": [
        "From WSL2 Ubuntu 24 with WSLg and required build packages installed, perform a clean install of the package from the repo or git source.",
        "Verify install exits 0 and does not print `EBADPLATFORM`.",
        "Verify the Linux helper binary exists at the documented package-local path and is executable."
      ],
      "passes": true,
      "regression_notes": "2026-03-13: Clean Linux npm install succeeded from a no-node_modules state without EBADPLATFORM. postinstall built .pi/extensions/generative-ui/native/linux/bin/pi-generative-ui-linux-helper and helper --version/self-test reported gtk3+webkit2gtk-4.1 on this WSL environment."
    },
    {
      "category": "functional",
      "description": "A headed interactive pi session can load the extension on Linux, open a native widget window, and receive a page message through `window.glimpse.send(...)`.",
      "steps": [
        "Launch a headed interactive `pi` session with this package loaded from the local repo path.",
        "Submit a prompt that triggers `visualize_read_me` and `show_widget`.",
        "Verify a native Linux widget window opens and remains visible while the pi session stays active.",
        "Interact with the widget so it sends a JSON payload through `window.glimpse.send(...)`.",
        "Verify the tool result shown in pi includes the returned payload."
      ],
      "passes": true,
      "regression_notes": "2026-03-13: Verified item 2 in a headed interactive `pi --no-extensions -e /home/devkit/.pi/agent/extensions/pi-generative-ui` session on DISPLAY=:99 after the ambient WSLg DISPLAY remained stale. `xwininfo -root -tree` showed the native `linux roundtrip` GTK/WebKit window while pi stayed interactive, and the tool result returned `messageData` = `{\"ok\":true,\"source\":\"xvfb-headed-pi\",\"backend\":\"linux-webview\"}` from `window.glimpse.send(...)`."
    },
    {
      "category": "functional",
      "description": "Streamed widget rendering updates appear incrementally in the headed pi session and finalize by executing deferred scripts once.",
      "steps": [
        "In the headed interactive `pi` session, submit a visual prompt that causes a streamed `show_widget` call with visible partial updates.",
        "Verify the widget window appears before the tool call is fully complete.",
        "Verify at least one intermediate DOM update is visible before final completion.",
        "Verify the finalized widget executes its deferred scripts after the final payload arrives."
      ],
      "passes": true,
      "regression_notes": "2026-03-13: Fixed streamed-window finalization so deferred scripts execute exactly once and execute() reuses an in-flight streamed window instead of re-sending the final payload. Verified in a headed `env DISPLAY=:101 pi --no-extensions -e /home/devkit/.pi/agent/extensions/pi-generative-ui` session: pi showed `show_widget stream probe` while still working, `xwininfo -root -tree` showed the native window before completion, ImageMagick captures on the live Xvfb display changed between shot1/shot2 (`compare -metric AE` = 877) before stabilizing at shot3 (`diff23=0`), and the returned widget payload reported `scriptRuns:1`, `finalStatus:\"finalized\"`, `rowCount:48`."
    },
    {
      "category": "functional",
      "description": "Widget lifecycle resolution on Linux preserves manual close and abort behavior.",
      "steps": [
        "Open a widget that does not immediately send a page message.",
        "Manually close the widget window and verify pi reports the expected close result.",
        "Open another widget, abort the active tool from pi, and verify the widget closes.",
        "Verify no orphan helper process remains after each resolution path."
      ],
      "passes": true,
      "regression_notes": "2026-03-13: Fixed aborted streamed-widget cleanup so assistant aborts close any in-flight Linux window before execute, and verified lifecycle semantics on Xvfb DISPLAY=:103. Manual close in a headed interactive `pi --no-extensions -e /home/devkit/.pi/agent/extensions/pi-generative-ui` session returned `Window closed by user.` with no remaining helper process; a separate headed Linux `pi --mode rpc` run aborted immediately after `tool_execution_start` returned `details.closedReason = \"Aborted.\"`, removed the native `rpc abort verify` window, and left no orphan helper."
    },
    {
      "category": "functional",
      "description": "Linux diagnostics clearly distinguish missing display, missing helper binary, and missing runtime prerequisites.",
      "steps": [
        "Exercise the extension with `DISPLAY` and `WAYLAND_DISPLAY` unavailable and verify the reported error names the missing GUI display.",
        "Exercise the extension with the Linux helper binary missing or non-executable and verify the reported error names that specific condition.",
        "Exercise the extension in a missing runtime/dependency scenario and verify the reported error includes actionable remediation guidance."
      ],
      "passes": true,
      "regression_notes": "2026-03-13: Hardened Linux diagnostics to avoid hanging display probes, added helper-path override support for fault injection, and verified `WSLG_REQUIRED` with DISPLAY unset, `BACKEND_BINARY_MISSING`, `BACKEND_BINARY_NOT_EXECUTABLE`, and `WEBKIT_RUNTIME_MISSING` plus remediation guidance via backend.checkSupport()."
    },
    {
      "category": "functional",
      "description": "Package metadata and documentation describe Linux/WSLg support, installation requirements, and verification workflow without breaking existing pi package behavior.",
      "steps": [
        "Review package metadata and README after the change.",
        "Verify the documented install path matches how pi packages are loaded and installed.",
        "Verify Linux prerequisites and the headed interactive pi verification workflow are documented for WSL2 + WSLg users."
      ],
      "passes": true,
      "regression_notes": "2026-03-13: Updated package metadata and README for Linux/WSLg support, documented `pi install git:github.com/Michaelliv/pi-generative-ui` plus headed local validation via `pi --no-extensions -e /home/devkit/.pi/agent/extensions/pi-generative-ui`, and narrowed published package contents with a `files` whitelist verified by npm pack --dry-run."
    },
    {
      "category": "functional",
      "description": "The macOS Glimpse backend path remains intact and regression-verified in an appropriate macOS environment.",
      "steps": [
        "In a macOS environment with the required toolchain, install the package and load it in pi.",
        "Trigger a visual prompt that opens a widget through the Glimpse backend.",
        "Verify the widget opens, can send a page message, and still follows the existing macOS lifecycle semantics."
      ],
      "passes": false,
      "regression_notes": "2026-03-13: Added scripts/verify-macos-glimpse.js as a deterministic pi RPC regression harness for a real macOS run, but this WSL2/Linux iteration could only verify script syntax, package gates, and the explicit non-macOS guard. Item remains blocked until the harness is executed in an actual macOS environment with the Glimpse backend."
    }
  ]
}
